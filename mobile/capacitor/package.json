{
  "name": "cloud-monitoring-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper Android (Capacitor) para o dashboard CloudV2.",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
