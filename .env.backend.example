# Public port exposed on EC2 host
BACKEND_PUBLIC_PORT=8008

# MQTT runtime
BROKER=a19mijesri84u2-ats.iot.us-east-1.amazonaws.com
MQTT_PORT=8883

# Frontend (Render) allowed to call API
CORS_ALLOWED_ORIGINS=https://SEU_FRONTEND.onrender.com

# Cross-domain auth cookie for Render -> AWS API
AUTH_COOKIE_SAMESITE=None
AUTH_COOKIE_SECURE=1
AUTH_BASE_URL=https://SEU_BACKEND_API

# Fixed admin seed
AUTH_FIXED_ADMIN_ENABLED=1
AUTH_FIXED_ADMIN_EMAIL=admin@exemplo.com
AUTH_FIXED_ADMIN_PASSWORD=troque-essa-senha

# Optional SMTP (keep console in initial setup)
AUTH_EMAIL_MODE=console
# AUTH_SMTP_HOST=smtp.gmail.com
# AUTH_SMTP_PORT=587
# AUTH_SMTP_USER=seu-email@gmail.com
# AUTH_SMTP_PASSWORD=senha-ou-app-password
# AUTH_SMTP_FROM=seu-email@gmail.com

# Optional hardening
# AUTH_TOKEN_PEPPER=um-segredo-longo-e-unico
# AUTH_SESSION_TTL_SEC=43200
